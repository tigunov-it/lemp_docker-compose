[mysqld]

skip-host-cache
skip-name-resolve

server-id=3
binlog_format=ROW
log_bin = mysql-bin.log
binlog_do_db = gb_hiload

collation-server     = utf8mb4_unicode_ci
character-set-server = utf8mb4

# add 640 file permissions